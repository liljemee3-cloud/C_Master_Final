name: c_master_pro
description: "الموسوعة الشاملة لتعلم لغة C من الألف إلى الياء"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
  
